#!/usr/bin/env bash
set -euo pipefail

if [[ "${1:-}" == "build" ]]; then
  shift
  exec /usr/bin/docker buildx build \
    --builder agent-builder \
    --network host \
    --allow network.host \
    "$@"
fi

exec /usr/bin/docker "$@"
