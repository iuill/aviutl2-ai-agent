#!/usr/bin/env bash

set -euo pipefail

codex_cli="${HOST_CODEX_BIN:?HOST_CODEX_BIN is not set}"

if [[ ! -x "${codex_cli}" ]]; then
  echo "Host Codex CLI is not executable: ${codex_cli}" >&2
  exit 127
fi

exec "${codex_cli}" \
  --dangerously-bypass-approvals-and-sandbox \
  "$@"
